#include <stdio.h>

int main() {
    // int rows = 26;
    // for (int i = 0; i < rows; i++) {
        
    //         printf(" %c", 'a' + i);

    // }
    int i=0;
    while (i<27)
    {
        printf(" %c", 'a' + i);
        i+=4;
    }
    return 0;
}
